<?xml version="1.0"?>
<!DOCTYPE article PUBLIC "-//OASIS//DTD DocBook XML V4.2//EN" "/usr/share/sgml/docbook/xml-dtd-4.2-1.0-14/docbookx.dtd" [
  <!ENTITY uw "University of Washington">
  <!ENTITY dept "Department of Bioengineering">
  <!ENTITY whitechuck '<systemitem class="systemname">whitechuck</systemitem>'>
]>
<article><title>Nag C Library Installation</title>
 <articleinfo>
    <revhistory>
      <revision>
	<revnumber>1.0</revnumber>
	<date>June 17, 2003</date>
	<authorinitials>afw</authorinitials>
	<revremark>Initial version.</revremark>
      </revision>
    </revhistory>
  <abstract>
      <para>
	For the time being, the SPK library (libspk) requires
	the Nag C Library.  This document explains the installation
	of this library on a RFPK Software Team workstation.
      </para>
  </abstract>
 </articleinfo>
  <sect1>
    <title>Installation</title>
    <para>
      As an ordinary user, download the Linux version of the 
      Nag C Library from 
      <systemitem class="systemname">whitechuck</systemitem>
      <screen>
cd /tmp
scp whitechuck:/opt/download/lux06dbt.Z .
      </screen>
    </para>
    <para>
      Become root, and install the library and header files:
      <screen>
su -

cd /tmp
tar xvzf lux06dbt.Z

cd cllux06db
cp libnagc* /usr/local/lib
cp include/* /usr/local/include

cd /usr/local/lib
ln -s libnagc.so.6 libnagc.so
      </screen>
    </para>
    <para>
      Set things up so that the runtime loader can find the
      shared library.  As root, edit the file
      <filename>/etc/ld.so.conf</filename> by adding the
      following line:
      <screen>
/usr/local/lib
      </screen>
    </para>
    <para>
      Then run <command>ldconfig</command> to update the cache
      for the runtime loader:
      <screen>
ldconfig -v
      </screen>
    </para>
    <para>
      Clean up:
      <screen>
cd /tmp
rm lux06dbt.Z
rm -rf cllux06db
      </screen>
    </para>
    <para>
      That should do it.
    </para>
  </sect1>
</article>
