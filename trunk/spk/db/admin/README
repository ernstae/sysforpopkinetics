ADMINISTRATIVE FILES FOR MANAGING SPKDB AND SPKTEST

extract.pl
    Script which creates two files:
    
        drop.sql: 
        schema.sql
    
    These two files are used to reinitialize the spktest database for
    testing purposes.  
    
    The script is typically run as follows:
    
        perl extract.pl spkdb <user>
    
    where <user> is a database username which has backup read privileges
    on the mysql table.

drop.sql
    sql commands to drop all tables in spkdb or spktest

schema.sql
    sql commands to create all tables in spkdb or spktest
